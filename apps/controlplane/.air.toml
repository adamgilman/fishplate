root = "."
tmp_dir = "/tmp/air"

[build]
  cmd = "go build -buildvcs=false -o /tmp/air/controlplane ./cmd/server/"
  bin = "/tmp/air/controlplane"
  include_ext = ["go"]
  exclude_dir = ["dist", "vendor"]
  exclude_regex = ["_test\\.go$"]
  delay = 1000

[log]
  time = true
